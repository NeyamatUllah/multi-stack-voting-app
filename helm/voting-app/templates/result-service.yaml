apiVersion: v1
kind: Service
metadata:
  name: result
  labels:
    app: result
    {{- include "voting-app.labels" . | nindent 4 }}
spec:
  selector:
    app: result
  ports:
    - name: http
      port: 80
      targetPort: 8080
