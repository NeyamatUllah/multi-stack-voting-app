apiVersion: v1
kind: Service
metadata:
  name: vote
  labels:
    app: vote
    {{- include "voting-app.labels" . | nindent 4 }}
spec:
  selector:
    app: vote
  ports:
    - name: http
      port: 80
      targetPort: 8080
