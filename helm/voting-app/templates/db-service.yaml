{{- if not .Values.externalDb.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    {{- include "voting-app.labels" . | nindent 4 }}
spec:
  selector:
    app: db
  ports:
    - port: 5432
      targetPort: 5432
{{- end }}
