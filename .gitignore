# .NET build artifacts
worker/obj/
worker/bin/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Node
node_modules/

# Environment files (never commit secrets)
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Terraform — never commit state or provider cache (state contains secrets)
.terraform/
terraform.tfstate
terraform.tfstate.backup
terraform.tfstate.*.backup
tfplan
*.tfplan
